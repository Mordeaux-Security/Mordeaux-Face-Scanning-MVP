# Cross-platform GPU Worker Dependencies
# Optimized for Windows AMD GPU with DirectML support, CPU fallback on macOS

# Core API framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# ONNX Runtime (cross-platform, includes CPU provider on all platforms)
# DirectML provider automatically available on Windows when onnxruntime-directml is installed
onnxruntime>=1.20.0

# Face detection and embedding
insightface>=0.7.3

# Image processing
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0

# HTTP client for health checks
httpx>=0.25.0

# Process management
psutil>=5.9.0

# Windows-specific modules (optional, only needed on Windows)
# pywin32>=306; sys_platform == "win32"

# Development and monitoring
python-multipart>=0.0.6